# ZH-EN-Translator
A machine translation system based on the Transformer architecture for Chinese-English translation.

You can start the model training with the following command:

```language
$ conda create --name translator python=3.7
$ conda activate translator
$ pip install -r [path to your requirements.txt]
$ cd [your path to the code]
$ sh train.sh
```
